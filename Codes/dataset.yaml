# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: ../Datasets/PCBData/group00041  # dataset root dir
train: /00041  # train images (relative to 'path') 128 images
val: /00041  # val images (relative to 'path') 128 images
# test:  # test images (optional)

# Classes
nc: 7  # number of classes
names: [ 'background', 'open', 'short', 'mousebite', 'spur', 'copper', 'pinhole']  # class names